# Task Locks

当前任务占用表。用于支持多个 flow 并行调度时避免领取同一个任务或改同一批文件。

## 当前占用

| 任务 ID | 模块 | 状态 | 执行标识 | 开始时间 | 最近心跳 | 修改范围 |
|---|---|---|---|---|---|---|

## 规则

- `TASK_LOCKS.md` 只记录当前占用，不记录历史。
- 任务完成、打回、阻塞或交接后必须移除对应占用。
- 存在其他 `执行标识` 的 `ACTIVE` 占用时不得领取。
